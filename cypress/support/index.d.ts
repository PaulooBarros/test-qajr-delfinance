/// <reference types="cypress" />

/**
 * Os tipos dos custom commands são declarados junto de cada `*.commands.ts`
 * (bloco `declare global`). Este arquivo só garante que o TypeScript enxergue
 * as tipagens do Cypress em todo o projeto.
 */
